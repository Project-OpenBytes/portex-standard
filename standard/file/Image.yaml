---
type: template
params:
  is_continous:
    required: false
    default: false
    options: [true, false]

  distort_status:
    required: false
    default: "RAW"
    options: ["RAW", "UNDISTORTED", "RECTIFIED"]

declaration:
  type: binary
