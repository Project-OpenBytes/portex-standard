---
type: template
parameters:
  - name: is_continous
    default: false
    options: [true, false]

  - name: distort_status
    default: "RAW"
    options: ["RAW", "UNDISTORTED", "RECTIFIED"]

declaration:
  type: binary
