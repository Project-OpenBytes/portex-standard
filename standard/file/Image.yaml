---
type: template
parameters:
  - name: is_continous
    default: false
    options: [true, false]

  - name: distort_status
    default: "RAW"
    options: ["RAW", "UNDISTORTED", "RECTIFIED"]

declaration:
  type: file.File
  extras:
    - name: height
      type: int32

    - name: width
      type: int32
