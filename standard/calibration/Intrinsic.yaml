---
type: template
params:
  coords:
    required: false
    default:
      type: float32

declaration:
  type: record
  fields:
    - name: cameraMatrix
      type: tensor
      shape: [3, 3]
      items: $params.coords

    - name: distortionCoefficients
      type: array
      items: $params.coords
