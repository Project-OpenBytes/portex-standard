---
type: template
params:
  shape:
    required: true

  dtype:
    required: true

  categoryIds:
    required: true

  categoryDelimiter:
    required: false
    default: null

  attributes:
    required: false
    default: null

  isTracking:
    required: false
    default: false
    options: [true, false]

declaration:
  type: record
  fields:
    - name: mask
      type: tensor
      shape: $params.shape
      dtype: $params.dtype

    - name: info
      existIf: $params.attributes != null
      type: array
      items:
        type: record
        fields:
          - name: id
            type: int32

          - name: attribute
            type: label.Attribute
            attributes: $params.attributes
