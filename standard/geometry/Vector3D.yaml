---
type: template
params:
  coords:
    required: false
    default:
      type: float32

declaration:
  type: record
  fields:
    - name: x
      +: $params.coords

    - name: y
      +: $params.coords

    - name: z
      +: $params.coords
