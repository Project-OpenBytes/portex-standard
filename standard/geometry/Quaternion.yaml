---
type: template
parameters:
  - name: coords
    default:
      type: float32

declaration:
  type: record
  fields:
    - name: w
      +: $coords

    - name: x
      +: $coords

    - name: y
      +: $coords

    - name: z
      +: $coords
