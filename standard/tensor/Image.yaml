---
type: template
params:
  shape:
    required: true

  dtype:
    required: true

  isContinous:
    required: false
    default: false
    options: [true, false]

  distortStatus:
    required: false
    default: "RAW"
    options: ["RAW", "UNDISTORTED", "RECTIFIED"]

declaration:
  type: tensor
  shape: $params.shape
  dtype: $params.dtype
