---
type: template
parameters:
  - name: shape

  - name: pixels

  - name: is_continous
    default: false
    options: [true, false]

  - name: distort_status
    default: "RAW"
    options: ["RAW", "UNDISTORTED", "RECTIFIED"]

declaration:
  type: tensor
  shape: $shape
  items: $pixels
