---
type: template
params:
  isContinous:
    required: false
    default: false
    options: [true, false]

  distortStatus:
    required: false
    default: "RAW"
    options: ["RAW", "UNDISTORTED", "RECTIFIED"]

  shape:
    required: true

  dtype:
    required: true

declaration:
  type: tensor
  shape: $params.shape
  dtype: $params.dtype
